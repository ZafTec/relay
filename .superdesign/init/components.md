# Shared UI components

No frontend implementation exists yet. Relay currently contains only the Deno
API and worker scaffold, so there are no reusable UI component source files to
include.

Planned primitives after the landing design is approved: Button, LinkButton,
StatusBadge, WindowFrame, CodeBlock, SectionKicker, VersionBadge, and
OperationalDiagram.
